- **Comandos de Processo:** `tasklist`, `taskkill`, `Get-Process` (PowerShell).
    
- **Comandos de Serviço:** `sc`, `net start/stop`, `Get-Service`.
    
- **Rede:** `ipconfig`, `netstat`, `ping`, `tracert`.
    
- **Sistema de Arquivos:** `dir`, `cd`, `copy`, `robocopy`, `chkdsk`.